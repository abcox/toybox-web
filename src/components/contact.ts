// todo: figure out swagger-codegen to generate nestjs/node/typescript client (includes models/interfaces)
export default interface Contact {
  id: string;
  name: string;
  email: string;
  phone: string;
}
